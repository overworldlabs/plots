# Translations

Plots ships with these locale files:
- `en_us`
- `pt_br`
- `es_es`
- `ru_ru`

Language files are generated in `mods/Stoshe_Plots/lang/` and can be customized there.

Set the active language through `Language` in your `config.json`.
